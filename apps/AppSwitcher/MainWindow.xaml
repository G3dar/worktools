<Window x:Class="AppSwitcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="App Switcher" Height="160" Width="520"
        WindowStyle="None" AllowsTransparency="True" Background="#00000000"
        Topmost="True"
        ResizeMode="CanResizeWithGrip"
        Loaded="Window_Loaded"
        Closing="Window_Closing"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        SizeChanged="Window_SizeChanged">
    <Border Background="#CC1E1E1E" CornerRadius="10" BorderBrush="#444" BorderThickness="1">
        <DockPanel LastChildFill="True">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Background="#222" Height="28">
                <ToggleButton x:Name="AlwaysOnTopToggle" Margin="6,4" Padding="6,2" Content="Always on top" Checked="AlwaysOnTopToggle_Checked" Unchecked="AlwaysOnTopToggle_Unchecked"/>
                <Button Content="☰" Margin="6,4" Padding="6,2" Click="MenuButton_Click"/>
                <TextBlock Text="Drag EXE/LNK or drop from running list" VerticalAlignment="Center" Foreground="#bbb" Margin="8,0" />
            </StackPanel>

            <UniformGrid x:Name="SlotsGrid" Columns="4" Margin="10" />
        </DockPanel>
    </Border>
</Window>

